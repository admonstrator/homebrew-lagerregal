name: Release

on:
  push:
    tags: ["v*"]

env:
  CARGO_TERM_COLOR: always
  TARGET: aarch64-apple-darwin

jobs:
  release:
    runs-on: macos-latest
    permissions:
      # Creating the Release, and committing the regenerated formula back to
      # the default branch. Both are in this repo, so the built-in
      # GITHUB_TOKEN is enough - no personal access token needed.
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          # The tag is checked out detached; the formula commit needs to land
          # on the default branch, so fetch enough history to switch to it.
          fetch-depth: 0

      - name: Derive version from tag
        id: version
        run: |
          version="${GITHUB_REF_NAME#v}"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Releasing $version"

      # A tag whose version disagrees with Cargo.toml would ship a binary
      # that reports a different version than the formula installs. Catch it
      # here rather than after publishing.
      - name: Verify tag matches Cargo.toml version
        run: |
          manifest="$(cargo metadata --no-deps --format-version 1 \
            | python3 -c 'import json,sys; print(json.load(sys.stdin)["packages"][0]["version"])')"
          if [ "$manifest" != "${{ steps.version.outputs.version }}" ]; then
            echo "::error::tag ${{ github.ref_name }} does not match Cargo.toml version $manifest"
            exit 1
          fi

      - name: Build
        run: cargo build --release --target "$TARGET"

      - name: Package
        id: package
        run: |
          version="${{ steps.version.outputs.version }}"
          archive="lagerregal-${version}-${TARGET}.tar.gz"
          tar -czf "$archive" -C "target/${TARGET}/release" lagerregal
          sha="$(shasum -a 256 "$archive" | cut -d' ' -f1)"
          echo "archive=$archive" >> "$GITHUB_OUTPUT"
          echo "sha256=$sha" >> "$GITHUB_OUTPUT"
          echo "$sha  $archive"

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.package.outputs.archive }}
          generate_release_notes: true

      # This repo doubles as its own Homebrew tap: the formula lives here
      # under Formula/, so a release only has to rewrite it and push. That
      # keeps everything in one repo and avoids needing a personal access
      # token for a second one.
      - name: Update formula
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
        run: |
          version="${{ steps.version.outputs.version }}"
          archive="${{ steps.package.outputs.archive }}"
          sha="${{ steps.package.outputs.sha256 }}"
          url="https://github.com/${GITHUB_REPOSITORY}/releases/download/${GITHUB_REF_NAME}/${archive}"

          git checkout "$DEFAULT_BRANCH"
          mkdir -p Formula
          cat > Formula/lagerregal.rb <<EOF
          # Generated by .github/workflows/release.yml on every tagged release.
          # Edits here are overwritten; change the workflow instead.
          class Lagerregal < Formula
            desc "Classify and browse your installed Homebrew packages by category"
            homepage "https://github.com/${GITHUB_REPOSITORY}"
            url "${url}"
            sha256 "${sha}"
            version "${version}"
            license "MIT"

            depends_on :macos
            depends_on arch: :arm64

            def install
              bin.install "lagerregal"
            end

            test do
              assert_match "lagerregal", shell_output("#{bin}/lagerregal --version")
            end
          end
          EOF

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/lagerregal.rb
          if git diff --staged --quiet; then
            echo "Formula already up to date."
          else
            # [skip ci] - a formula-only commit has nothing for CI to check.
            git commit -m "lagerregal ${version} [skip ci]"
            git push origin "$DEFAULT_BRANCH"
          fi
